<resources>

    <color name="white_opaque">#FFFFFFFF</color>
    <color name="pitch_black">#FF000000</color>

    <!-- Base application theme.-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar.FullScreen">
    </style>
    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:background">@color/pitch_black</item>
        <item name="android:windowBackground">@color/pitch_black</item>
        <item name="android:colorBackground">@color/pitch_black</item>
        <item name="android:textColor">@color/white_opaque</item>
    </style>

    <!-- Used only with ExoPlayer => API >= 14 -->
    <style name="ExoRootTheme" parent="android:Theme.Holo.Light.NoActionBar">
    </style>

    <!-- Used only with ExoPlayer => API >= 14 -->
    <style name="ExoPlayerTheme" parent="@style/ExoRootTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Alert Dialog -->
    <style name="AppDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
    </style>

    <style name="AppSpinner" parent="Widget.AppCompat.Spinner">
        <item name="android:textColor">@color/white_opaque</item>
        <item name="android:colorBackground">#222222</item>
        <item name="android:textSize">20dp</item>
    </style>

    <!-- Buttons -->
    <style name="ButtonJDJ" parent="Base.Widget.AppCompat.Button">
        <item name="android:textColor">#DDDDDD</item>
        <item name="android:background">#222222</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:padding">1dp</item>
    </style>

</resources>
